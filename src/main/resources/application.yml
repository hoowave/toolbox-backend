# Spring Datasource Configuration
spring:
  application:
    name: toolbox
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/d_closeout_market
    username: hoowave
    password:

  # JPA and Hibernate Configuration
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        bytecode:
          format_sql: true